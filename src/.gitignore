node_modules/
dist/
*.tsbuildinfo
data/
.env
# .env backups (some env-import/sed steps leave .env.bak.<ts>). Ignore them so a
# stray backup never marks the prod tree "dirty" and silently blocks autodeploy.
.env.bak*
.env.*.bak
*.db
*.db-journal
*.db-wal
*.db-shm
.turbo/
.DS_Store
*.log
coverage/
.vite/
