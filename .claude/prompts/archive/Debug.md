I want to make a utility for logging better names of objects in the system logs, instead of their GUIDs.
Currently all the messages are referencing a GUID of some kind, ie:
DEBUG [TranscoderService] Transcode progress [84cf5df9-c9f8-49b2-9407-d89e9adf91b9]: 26.0%
...etc.

I'd like you to make a mode, when in debug, that calls a utility, passing that given ID to it, and the utility will look up that guid in a cache, which has the transcribed movie title, or whatever the name of the object is. This should be a service that utilizes an in-memory cache with a semi-low TTL (ie. 30mins or 1 hour) for GUIDs-to-name lookups. If the name isn't in the cache, it should try to find it in the database, or wherever it mmight currently have it, and put it in the cache.
Then, everywhere log messages print out movie GUIDs, have it call this service to try to resolve the movie ID's actual title, or otherwise its filename. It should resolve to its actual Title (ie. if metadata for it exists), or otherwise it should use the filename it has for the movie. For now, let's start with movie GUID translation, but our GUID transcriber will later support other object types, so keep that in mind, and design a system that is flexible, and can adapt or delegate the calls to the correct underlying systems, to resolve an ID, for a given type (the services or type should system should implement that "resolveGuid(guid)" method, if they support it, and the logging utility can just try to call that if it exists, or continue just using the given guid as a result).
Integrate the GUID-to-name transcription service and cache, and update all movie GUID references to use it, so that we will pull the movie names, and print those in the logs, instead of the GUIDs. The GUID can also still be printed in parenthesis, after the title, if the title exists, or otherwise just print the GUID instead.