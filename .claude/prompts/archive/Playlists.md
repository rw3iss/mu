
On the Playlists page, and playlists api endpoints, can you add a query flag to the endpoint to tell it to also return the summary list of movies in each playlist, including their title, thumnail, and year, if requested?
Then, on the Playlists page, load the playlists with the query flag, so it returns the list of playlists, each with the list of movie.
Then, within the playlist card items, show each movie as a thumbnail for the movie in a grid, left to right, 3x2 movies, as a preview of the movies in the list in the card's "thumbnail" area (where the icon current is). If there is no posted, show an abbreviation of the title, and format the year under it nicely, a bit smaller.
Can you also style the playlist cards themselves so they're a little bigger, to fit the 3x2 movie posters, and make the playlist title a little nicer. Remove the rounded corners on card top corners, keep the bottoms rounded.
