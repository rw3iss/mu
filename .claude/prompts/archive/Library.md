We want the user to be able to add multiple directories, and also 'select' a directory.
Let's refactor the "Media Library Path" sections, both in the Onboarding/Setup and also the Settings > Library page, to support multiple entries, where each entry can allow the user to edit the path, and also select a new folder to override that path for that entry.
In the current 'Media Library Path' sections, add the new single-folder select control, which should be a new custom control with a text input on the left, and a 'Select...' button attach to it on the right. It should also include a hidden 'Scan' button to the right of the folder entry, which will show once the user saves the Settings, so they can scan that folder, if it's valid.
When the user clicks Select, it should open the file open dialog to select a folder, which when selected should set the folder path in that textarea.
There should always be an 'Add another folder' button should under the list of controls/selected folders above, in this section. When the user clicks this button, it should add another empty entry of the custom control above, to select another folder.
Once the user has added and selected multiple folders, and clicks Continue or Save, it should filter out any of the directories which did not have any values (ie. trim them and if they are empty strings or undefined), and remove them from the UI list of custom controls, and leave only the entries which had valid paths or any text at all. Then, it should save the remaining valid folders as an array of folder string to the "media paths" property in the Settings. Ensure the backend is updated now to support handling multiple media paths in the data entity's CRUD operations, instead of just a single media path. Once the user saves or updates their media paths, the frontend UI should reflect that the paths are saved by showing the 'Scan' button to the right of each of the Media path input components we created. The user should be able to click 'scan' for a single media path. Modify the backend scan endpoint to support specifying a single or specific media path to scan, instead of scanning all of them (it should still support scanning all of them if no specific path is given). Then, when the user clicks 'scan' on a Media path row item above, that they have saved, it should request that scan begins, show the toast notification, and show the message under the Input textarea and Select button, in that component, that says that media path is being scanned, and show the number of items it found that are being processed, just like it does for the main action of "Scan Now" does, for all media paths. Ensure the single and bulk scan operations will show the number of items it is processing when the scan begins. You can use common components and shared code if possible.
Possibly later we could store the number of scanned or existing items in a media path directory, so maybe the settings could be modified to store an object entry for each media path?
The user could enter strings, and if the backend sees a plain string, it knows it a new directory, and should try to save it (create a full "MediaPath" object from it). In that object it can add status properties of whether the directory is being scanned, how many items are being processed, how many are finished processing, etc. Then the frontend can see either the plain string, or the full objects, and know if that media path is new (hasn't been saved yet, ie. don't show the 'scan' button on that item), or if it has been saved, and is being scanned or not (based on the full object's properties for that media path entry). That is a good approach. Can you modify the scanning system to support the array entries, and have the client frontend and backend differentiate between the strings and objects, depending on whether a media path has been saved, and is being scanned or not, including it's scan progress details?

--------------------------------------------------------------------------------
